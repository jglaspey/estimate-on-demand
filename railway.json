{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "echo 'Starting custom build...' && rm -rf node_modules/.cache && npm ci --production=false && echo 'npm ci complete' && npx prisma generate && echo 'prisma generate complete' && npm run build && echo 'build complete'"
  },
  "deploy": {
    "startCommand": "npx prisma migrate deploy && npm start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
